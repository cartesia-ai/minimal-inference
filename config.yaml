model_path: /shared/data/llm-post-training/model_store/Mistral-7B-Instruct-v0.3
device: cuda
dtype: bfloat16

server:
  host: 0.0.0.0
  port: 8000

kv_proj_dim: 0  # JL random projection dim (0 = disabled, e.g. 96 for compression)

scheduler:
  max_seq_len: 4096
  max_batch_size: 40
  page_size: 16
  use_flashinfer: true
