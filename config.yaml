model_path: /shared/data/llm-post-training/model_store/Mistral-7B-Instruct-v0.3
device: cuda
dtype: bfloat16

server:
  host: 0.0.0.0
  port: 8000

scheduler:
  max_seq_len: 4096
  max_batch_size: 40
  page_size: 16

# JL random projection KV cache compression (padded path only).
# Projects K and Q to lower dim; V stays full. E.g. 32 for 2x K-cache compression on head_dim=64.
# kv_proj_dim: 32
